/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
domains: ['via.placeholder.com', '127.0.0.1', 'localhost', 'yugo-glasgow.sakura.ne.jp'],
  },
}

module.exports = nextConfig
