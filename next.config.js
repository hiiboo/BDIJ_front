/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
domains: ['via.placeholder.com', '127.0.0.1', 'localhost', 'hibo.sakura.ne.jp'],
  },
}

module.exports = nextConfig
